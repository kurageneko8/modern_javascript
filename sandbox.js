var radius = 20;

var area = Math.PI * radius * radius;
var area = Math.PI * Math.pow(radius,2);
area

var fullName = 'Larry Ullman';
// fullName.indexOf('a');
fullName.indexOf('L');
fullName.lastIndexOf('a');
fullName.lastIndexOf('a',5);